# Environment variables for Docker Compose
# yahan par Docker Compose ke liye environment variables hain

# Database Configuration - yahan par database settings hain
DB_ROOT_PASSWORD=rootpassword123
DB_NAME=findaspot
DB_USER=findaspot_user
DB_PASSWORD=password123

# SMTP Email Configuration - yahan par email settings hain
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_FROM=noreply@findaspot.com
SMTP_USE_TLS=true

# JWT Configuration - yahan par security settings hain
JWT_SECRET=your-super-secret-jwt-key-change-in-production-environment

# Application Configuration - yahan par app settings hain
SPRING_PROFILES_ACTIVE=docker